let x = 0
let move = document.querySelector('.sliders')

function left() {
    x = x + 1200
    move.style.left = `${x}px`



}

function right() {
    x = x - 1200
    move.style.left = `${x}px`
    s

}